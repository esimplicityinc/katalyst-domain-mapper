# FOE API (Elysia on port 3001) - No authentication required
API_BASE_URL=http://localhost:3001

# FOE Web UI (React/Vite on port 3000)
FRONTEND_URL=http://localhost:3000

# Browser mode (set to false for headed debugging)
HEADLESS=true

# Debug mode for TUI testing (optional)
DEBUG=false
