---
id: CAP-027
title: "Reusable AI Chat Component Library"
tag: "@CAP-027"
category: Technical
status: stable
description: "Shared React component library (@katalyst/chat) providing a drop-in AI chat UI and headless hook for SSE streaming, session management, and question/answer tool handling across all AI-powered pages."
---

# CAP-027: Reusable AI Chat Component Library

## Description

The `@katalyst/chat` package provides a shared, configurable chat component library that eliminates duplicated AI chat code across the application. It exports `OpenCodeChat` (a styled React component) and `useOpenCodeChat` (a headless hook), enabling consistent AI chat experiences on the FOE assessment, DDD modeling, and Taxonomy management pages. The component handles SSE streaming, markdown rendering with syntax highlighting, session lifecycle, and the question/answer tool pattern — all configurable via props like `agentId` and `accentColor`.

## Key Features

- **`OpenCodeChat` component**: Drop-in chat UI with markdown rendering, SSE streaming, suggestion chips, dark mode, and accent color variants (`purple`, `blue`, `green`, `rose`)
- **`useOpenCodeChat` hook**: Headless state management for session creation, message sending, SSE event handling, and question/answer tool support
- **`createClient` API helper**: Low-level wrapper around the OpenCode SDK for health checks and session operations
- **Agent targeting**: `agentId` prop routes conversations to different AI agents (`foe-assessment-coach`, `ddd-domain-mapper`, `taxonomy-architect`)
- **3 consumers**: FOEChat, DDDChat, and TaxonomyChat all use `@katalyst/chat` with zero duplicated logic

## Architecture

```
@katalyst/chat
├── src/
│   ├── components/OpenCodeChat.tsx   (styled component)
│   ├── hooks/useOpenCodeChat.ts      (headless hook)
│   ├── api/opencode-client.ts        (SDK wrapper)
│   ├── types.ts                      (ChatMessage, ChatConfig)
│   └── index.ts                      (public exports)
│
Consumers:
├── FOEChat.tsx      → agentId: "foe-assessment-coach"
├── DDDChat.tsx      → agentId: "ddd-domain-mapper"
└── TaxonomyChat.tsx → agentId: "taxonomy-architect"
```

## User Types

- **UT-002** (Platform Engineer) — Integrates the chat component into new pages by importing from `@katalyst/chat` and configuring via props
- **UT-001** (Engineering Team Lead) — Interacts with the chat UI to get AI coaching on scan reports, domain models, and taxonomy

## Related Capabilities

- **CAP-017** (AI Assessment Coaching) — FOEChat uses CAP-027 to deliver assessment coaching
- **CAP-025** (Taxonomy & User Type Management UI) — TaxonomyChat and UserTypeChat use CAP-027

## Related Artifacts

- **User Stories**: US-080, US-088
- **Road Items**: (retroactive — built as part of ROAD-044/046 work)
